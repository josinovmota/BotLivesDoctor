#!/bin/bash

dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
cd $dir/..
path() {
  echo $1
}

################################################################################
# IO
################################################################################

. $(path $dir/io.sh)

################################################################################
# Update local bot version
################################################################################

io_info "updating local bot version ...\n"
git fetch
git pull
io_success "local bot version updated\n"
